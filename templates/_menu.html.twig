{% set action = app.request.attributes.get("_route") %}
<ul class="navbar-nav me-auto mb-2 mb-lg-0">
    <li class="nav-item">
        <a href="{{ path('patrimoine_index') }}" class="nav-link" aria-current="page">
            <i class="ti ti-search"></i> Rechercher
        </a>
    </li>
    {% if is_granted('ROLE_PATRIMOINE_ADMIN') %}
        <li class="nav-item">
            <a href="{{ path('patrimoine_new') }}" class="nav-link">
                <i class="ti ti-plus"></i> Ajouter un patrimoine</a>
        </li>
        <li class="nav-item"><a href="{{ path('patrimoine_type_index') }}" class="nav-link">Types</a></li>
        <li class="nav-item"><a href="{{ path('patrimoine_localite_index') }}" class="nav-link">Localités</a></li>
        <li class="nav-item"><a href="{{ path('patrimoine_statut_index') }}" class="nav-link">Statuts</a></li>
    {% endif %}
    {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
        <li class="nav-item">
            <a href="{{ path('app_login') }}" class="nav-link"><i class="ti ti-login-2"></i>
                Se connecter</a>
        </li>
    {% else %}
        <li class="nav-item"><a href="{{ path('app_logout') }}" class="nav-link"><i class="ti ti-logout-2"></i>
                Déconnexion</a>
        </li>
    {% endif %}
</ul>
