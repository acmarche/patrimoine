{{ form_start(form) }}
{{ form_errors(form) }}
{{ form_row(form.nom) }}
<div class="row">
    <div class="col-6">
        {{ form_row(form.rue) }}
    </div>
    <div class="col-6">
        {{ form_row(form.numero) }}
    </div>
</div>
<div class="row">
    <div class="col-6">
        {{ form_row(form.codePostal) }}
    </div>
    <div class="col-6">
        {{ form_row(form.localite) }}
    </div>
</div>
<div class="row">
    <div class="col-6">
        {{ form_row(form.typePatrimoine) }} </div>
    <div class="col-6">
        {{ form_row(form.statut) }}</div>
</div>
{{ form_row(form.descriptif) }}
{{ form_row(form.commentaire) }}

<button class="btn btn-primary mr-1 mt-3" type="submit"><i class="ti ti-device-floppy" aria-hidden="true"></i>
    Sauvegarder
</button>

{{ form_end(form) }}
