<div class="d-grid gap-2 d-md-block mb-3">
    <a href="{{ path('patrimoine_edit', { 'id': patrimoine.id }) }}" role="button"
       class="btn btn-primary"><i
                class="ti ti-pencil"></i> Editer</a>

    <a href="{{ path('patrimoine_images', { 'id': patrimoine.id }) }}" role="button"
       class="btn btn-primary"><i class="ti ti-photo"></i> Images</a>

    <a href="{{ path('patrimoine_localisation_edit', { 'id': patrimoine.id }) }}" class="btn btn-primary" role="button">
        <i class="ti ti-map"></i> Géolocalisation</a>

    <a href="{{ path('patrimoine_index') }}" class="btn btn-primary" role='button'><i class="ti ti-list"></i>
        Retour à la Liste</a>

    <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
        <i class="ti ti-trash"></i> Supprimer
    </button>
</div>
<br/><br/>
