<a href="{{ path('patrimoine_statut_edit', { 'id': statut.id }) }}" role="button"
   class="btn btn-primary"><i
            class="ti ti-pencil"></i> Editer</a>

<a href="{{ path('patrimoine_statut_index') }}" class="btn btn-primary" role='button'><i class="ti ti-list"></i>
    Retour à la Liste</a>

<button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
    <i class="ti ti-trash"></i> Supprimer
</button>

<br /><br />
